import { ArrowLeft, Sparkles } from 'lucide-react';
import { Link, useLocation } from 'react-router-dom';
export function ComingSoonPage() { const location = useLocation(); const title = location.pathname.includes('notifications') ? 'Notifications' : location.pathname.includes('menu') ? 'Menu management' : location.pathname.includes('settings') ? 'Shop settings' : 'This module'; return <div className="section-shell py-20"><Link to="/" className="inline-flex items-center gap-2 text-sm font-semibold text-muted hover:text-espresso"><ArrowLeft size={16} /> Back home</Link><div className="surface mx-auto mt-8 max-w-xl px-8 py-16 text-center"><div className="mx-auto flex h-14 w-14 items-center justify-center rounded-2xl bg-oat text-ember"><Sparkles /></div><p className="eyebrow mt-6">Next on the campus table</p><h1 className="mt-3 font-display text-5xl text-espresso">{title}.</h1><p className="mx-auto mt-4 max-w-md text-sm leading-6 text-muted">This capability is Coming Soon. The current slice keeps the experience honest while the underlying workflow is built.</p></div></div>; }
