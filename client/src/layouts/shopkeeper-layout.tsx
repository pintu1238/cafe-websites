import { LayoutDashboard, ListOrdered, Settings2, Utensils } from 'lucide-react';
import { NavLink, Outlet } from 'react-router-dom';
import { Navbar } from '../components/navbar';
import { cn } from '../lib/utils';

export function ShopkeeperLayout() { const links = [['/shopkeeper', 'Order board', ListOrdered], ['/shopkeeper/menu', 'Menu', Utensils], ['/shopkeeper/settings', 'Settings', Settings2]] as const; return <div className="min-h-screen bg-[#f4eee5] text-ink"><Navbar /><div className="section-shell grid gap-8 py-8 md:grid-cols-[220px_1fr]"><aside className="hidden md:block"><div className="sticky top-28 rounded-[1.5rem] border border-espresso/10 bg-white p-3 shadow-card"><p className="px-3 pb-3 text-xs font-bold uppercase tracking-[0.18em] text-ember">Operator desk</p>{links.map(([to, label, Icon]) => <NavLink key={to} to={to} end={to === '/shopkeeper'} className={({ isActive }) => cn('mb-1 flex items-center gap-3 rounded-2xl px-3 py-3 text-sm font-semibold transition', isActive ? 'bg-espresso text-cream' : 'text-muted hover:bg-oat hover:text-espresso')}><Icon size={17} />{label}</NavLink>)}</div></aside><main className="min-w-0"><Outlet /></main></div><nav className="fixed inset-x-0 bottom-0 z-30 border-t border-espresso/10 bg-cream/95 px-5 py-3 backdrop-blur-xl md:hidden"><div className="mx-auto flex max-w-md items-center justify-around">{links.map(([to, label, Icon]) => <NavLink key={to} to={to} end={to === '/shopkeeper'} className={({ isActive }) => cn('flex flex-col items-center gap-1 text-[10px] font-semibold', isActive ? 'text-ember' : 'text-muted')}><Icon size={18} />{label}</NavLink>)}</div></nav></div>; }
